program example
begin
    var i : int := -1 + 5;
    var j : ref(int) := ref(i);
    var k : ref(ref(int)) := ref(j);

    var x : int := deref(deref(k)) + 5;

    procedure swap (a: ref(int), b : ref(int))
    begin
        var tmp : int := 5;
        var tmp2 : float := 5.5;
        var i : ref(int) := ref(tmp)
    in
    end;

    procedure test(a : int, b : int) : int
    begin
        var c : int
    in
    end;

    var y : int := 5 + test(i, j)
end
