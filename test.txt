program example
begin
    var i : int := -1 + 5;
    var j : ref(int) := ref(i);
    var k : ref(ref(int)) := ref(j);

    var x : int := deref(deref(k)) + 5;

    procedure swap (a: ref(int), b : ref(int))
    begin
        var tmp : int
    in
    end;

    procedure swap()
    begin
    end
end
