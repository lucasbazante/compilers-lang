program example
begin
	var i : int;
	var j := 5.5;
	// var k : int := j * 7;
	// var x := i;
	// var r_i : ref(int) := ref(x);

	// struct Exemplo {
	// 	x : int
	// };
	//
	// var ex := new Exemplo . x + 5 + -7
	
	procedure main()
	begin
		// i := ex.x + 55 * 3;
		// r_i := ref(i);

		if i > 5 && i >= 7 then
			while i < 15 do
				i := i + 1;
				j := j - 1
			od
		else
			i := 7
		fi
	end
end
